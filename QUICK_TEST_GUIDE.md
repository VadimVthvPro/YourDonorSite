# 🧪 БЫСТРАЯ ПРОВЕРКА ИЗМЕНЕНИЙ

## 1️⃣ Перезапуск сервера

```bash
cd /Users/VadimVthv/Your_donor/website/backend
python3 app.py
```

---

## 2️⃣ Очистка кэша

**Safari/Chrome:** `Cmd + Shift + R`

---

## 3️⃣ Проверка функций

### ✅ Редактирование запроса
1. Войти как медцентр (ID: 41, пароль: doctor2024)
2. Запросы крови → нажать "Редактировать"
3. Должно открыться окно с данными

### ✅ Создание с количеством доноров
1. Запросы крови → "Создать запрос"
2. Выбрать "Конкретное количество"
3. Ввести число (например, 5)
4. Создать

### ✅ Белый фон модала
1. Создать запрос → проверить фон белый

### ✅ Новый дизайн карточек
1. Запросы крови → проверить:
   - Левая цветная полоса
   - Прогресс-бар (если есть откликов)
   - Аватары доноров (круглые с инициалами)

### ✅ Инициалы ВВО
1. Войти как донор
2. Правый верхний угол → проверить инициалы

### ✅ Написать донору
1. Медцентр → Запросы крови
2. Карточка с откликами → кнопка "Написать"
3. Должно открыться окно сообщения

---

## 4️⃣ Диагностика 403

### Если донор НЕ может откликнуться:

1. Откройте терминал где запущен Flask
2. Попробуйте откликнуться на запрос
3. Найдите строку:

```
❌ 403 FORBIDDEN: respond_to_blood_request требует 'donor', 
но user_type='XXXX'
```

4. Скопируйте и отправьте мне ПОЛНОСТЬЮ

---

## 5️⃣ Проверка БД

```bash
cd /Users/VadimVthv/Your_donor/website/backend
python3 << 'PYEOF'
import psycopg2
import os
from dotenv import load_dotenv

load_dotenv()

conn = psycopg2.connect(
    host=os.getenv('DB_HOST'),
    database=os.getenv('DB_NAME'),
    user=os.getenv('DB_USER'),
    password=os.getenv('DB_PASSWORD')
)

cur = conn.cursor()

# Проверяем новые колонки
cur.execute("SELECT needed_donors, current_donors, auto_close FROM blood_requests LIMIT 1;")
print("✅ Колонки для учёта доноров существуют")

# Проверяем таблицу donation_history
cur.execute("SELECT COUNT(*) FROM donation_history;")
count = cur.fetchone()[0]
print(f"✅ Таблица donation_history: {count} записей")

cur.close()
conn.close()
PYEOF
```

---

## 6️⃣ Ожидаемые результаты

✅ **Редактирование:** Окно открывается, данные загружены  
✅ **Количество доноров:** Поле показывается/скрывается  
✅ **Белый фон:** Модал с белым фоном  
✅ **Дизайн:** Карточки элегантные, левая полоса, прогресс  
✅ **Инициалы:** ВВО (или ваши реальные)  
✅ **Сообщения:** Модал открывается, форма заполнена

---

## 🚨 Если что-то не работает

1. Проверьте логи Flask
2. Проверьте консоль браузера (F12)
3. Скопируйте ошибку и отправьте мне

**Логи Flask должны показывать:**
```
✓ Авторизация OK
✓ Данные медцентра загружены
✓ Запросы крови загружены
```
