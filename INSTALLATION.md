# Инструкция по установке

Пошаговое руководство по установке и запуску платформы "Твой Донор".

## Перед началом

Убедитесь, что у вас установлены все необходимые компоненты:
- Python 3.8 или выше
- PostgreSQL 12 или выше
- Git (для клонирования репозитория)
- Доступ к интернету

Полный список требований см. в файле `REQUIREMENTS.md`.

---

## Шаг 1: Клонирование репозитория

```bash
# Клонируйте проект
git clone <repository-url>

# Перейдите в директорию проекта
cd Your_donor
```

---

## Шаг 2: Установка Python зависимостей

```bash
# Перейдите в папку backend
cd website/backend

# Установите зависимости
pip3 install -r requirements.txt

# Проверьте установку
python3 -c "import flask, psycopg2, telegram; print('Все зависимости установлены')"
```

Если возникают ошибки при установке `psycopg2-binary`:
```bash
# На Ubuntu/Debian:
sudo apt-get install python3-dev libpq-dev

# На macOS:
brew install postgresql

# Затем повторите установку:
pip3 install psycopg2-binary
```

---

## Шаг 3: Настройка PostgreSQL

### 3.1 Создание базы данных

```bash
# Подключитесь к PostgreSQL
psql -U postgres

# В консоли PostgreSQL выполните:
CREATE DATABASE your_donor;
\q
```

### 3.2 Выполнение миграций

```bash
# Убедитесь, что вы в папке website/backend
cd /path/to/Your_donor/website/backend

# Выполните основную миграцию
psql -U postgres -d your_donor -f create_database.sql

# Выполните дополнительные миграции
psql -U postgres -d your_donor -f migrations/add_donation_stats_to_users.sql
psql -U postgres -d your_donor -f migrations/add_donor_count.sql
psql -U postgres -d your_donor -f migrations/add_hidden_to_responses.sql
psql -U postgres -d your_donor -f migrations/create_donation_history_table.sql
psql -U postgres -d your_donor -f migrations/update_messaging_system.sql
psql -U postgres -d your_donor -f migrations/cleanup_messages_table.sql
psql -U postgres -d your_donor -f migrations/fix_message_sender_role.sql
psql -U postgres -d your_donor -f migrations/remove_medical_certificate_fields.sql
```

### 3.3 Проверка установки БД

```bash
# Подключитесь к базе
psql -U postgres -d your_donor

# Проверьте список таблиц
\dt

# Вы должны увидеть:
# - users
# - medical_centers
# - blood_requests
# - donation_responses
# - conversations
# - messages
# - message_templates
# - donation_history
# - telegram_verification_codes

# Выйдите
\q
```

---

## Шаг 4: Создание Telegram бота

### 4.1 Получение токена

1. Откройте Telegram и найдите @BotFather
2. Отправьте команду `/newbot`
3. Следуйте инструкциям:
   - Введите имя бота (например: "Твой Донор Bot")
   - Введите username бота (например: "TvoyDonorZdesBot")
4. Скопируйте полученный токен (формат: `1234567890:ABCdefGHI...`)

### 4.2 Настройка команд бота (опционально)

Отправьте @BotFather команду `/setcommands` и выберите вашего бота, затем отправьте:

```
start - Начать работу с ботом
help - Справка по командам
link - Привязать аккаунт
status - Проверить статус привязки
myid - Получить Telegram ID
```

---

## Шаг 5: Конфигурация переменных окружения

### 5.1 Создание .env файла

```bash
# Перейдите в папку backend
cd /path/to/Your_donor/website/backend

# Скопируйте пример конфигурации
cp env_example.txt .env
```

### 5.2 Заполнение .env файла

Откройте файл `.env` в текстовом редакторе и заполните следующие значения:

```env
# База данных
DB_HOST=localhost
DB_PORT=5432
DB_NAME=your_donor
DB_USER=postgres
DB_PASSWORD=ВАШ_ПАРОЛЬ_POSTGRESQL

# Безопасность
# Сгенерируйте секретный ключ:
# python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=ВАШЕ_ЗНАЧЕНИЕ_ЗДЕСЬ

# Мастер-пароль для регистрации медцентров
MASTER_PASSWORD=ВАШ_МАСТЕР_ПАРОЛЬ

# Telegram
TELEGRAM_BOT_TOKEN=ВАШ_ТОКЕН_ОТ_BOTFATHER

# URLs
# Для локальной разработки:
WEBSITE_URL=http://localhost:8080
APP_URL=http://localhost:8080

# Для продакшена:
# WEBSITE_URL=https://tvoydonor.by
# APP_URL=https://tvoydonor.by

# Сервер
FLASK_DEBUG=true
PORT=5001
```

**Важно:** Для Telegram WebApp кнопки ОБЯЗАТЕЛЕН HTTPS! В разработке кнопка работать не будет, используйте продакшен URL.

---

## Шаг 6: Загрузка данных медицинских центров (опционально)

Если у вас есть список медицинских центров:

```bash
cd /path/to/Your_donor/website/backend

# Создайте или обновите файл data/medcenters.json
# Затем выполните:
python3 build_medical_json.py
```

Структура `data/medcenters.json`:
```json
[
  {
    "id": 1,
    "name": "Название медцентра",
    "address": "Адрес",
    "phone": "+375291234567",
    "region": "Минск"
  }
]
```

---

## Шаг 7: Запуск приложения

### 7.1 Для разработки (3 терминала)

**Терминал 1: Flask API**
```bash
cd /path/to/Your_donor/website/backend
python3 app.py
```
Вы должны увидеть:
```
==================================================
🩸 Твой Донор - API Server
==================================================
Порт: 5001
БД: your_donor
==================================================
 * Running on http://127.0.0.1:5001
```

**Терминал 2: Telegram бот**
```bash
cd /path/to/Your_donor/website/backend
python3 telegram_bot.py
```
Вы должны увидеть:
```
Application started
```

**Терминал 3: Frontend HTTP сервер**
```bash
cd /path/to/Your_donor/website
python3 -m http.server 8080
```
Вы должны увидеть:
```
Serving HTTP on 0.0.0.0 port 8080 (http://0.0.0.0:8080/) ...
```

### 7.2 Для запуска в фоне (продакшен)

```bash
cd /path/to/Your_donor/website/backend

# Запуск Flask API
nohup python3 app.py > flask.log 2>&1 &

# Запуск Telegram бота
nohup python3 telegram_bot.py > telegram_bot.log 2>&1 &
```

Для продакшена рекомендуется использовать systemd или supervisor для управления процессами.

---

## Шаг 8: Проверка установки

### 8.1 Проверка API

Откройте браузер или используйте curl:
```bash
curl http://localhost:5001/api/health
```

Ожидаемый ответ:
```json
{"status": "ok"}
```

### 8.2 Проверка Frontend

Откройте браузер и перейдите по адресу:
```
http://localhost:8080
```

Вы должны увидеть главную страницу платформы "Твой Донор".

### 8.3 Проверка Telegram бота

1. Откройте Telegram
2. Найдите вашего бота по username
3. Отправьте команду `/start`
4. Бот должен ответить приветственным сообщением

---

## Шаг 9: Создание тестовых данных (опционально)

### 9.1 Регистрация тестового донора

1. Перейдите на `http://localhost:8080`
2. Нажмите "Зарегистрироваться"
3. Заполните форму:
   - ФИО: Тестовый Донор
   - Email: donor@test.com
   - Телефон: +375291234567
   - Пароль: test123
   - Группа крови: O+
   - Регион: Минск

### 9.2 Регистрация тестового медцентра

1. Перейдите на `http://localhost:8080/medcenter_login.html`
2. Нажмите "Зарегистрироваться"
3. Заполните форму:
   - Название: Тестовый медцентр
   - Адрес: ул. Тестовая 1
   - Телефон: +375291234567
   - Email: medcenter@test.com
   - Регион: Минск
   - Мастер-пароль: (значение из .env)

---

## Устранение проблем

### Ошибка: "Can't connect to PostgreSQL"

**Решение:**
1. Проверьте, что PostgreSQL запущен:
   ```bash
   # Linux:
   sudo systemctl status postgresql
   
   # macOS:
   brew services list
   ```
2. Проверьте настройки в .env файле
3. Попробуйте подключиться вручную:
   ```bash
   psql -U postgres -d your_donor
   ```

### Ошибка: "Module not found"

**Решение:**
```bash
# Переустановите зависимости
pip3 install -r requirements.txt --force-reinstall
```

### Ошибка: "Telegram bot doesn't respond"

**Решение:**
1. Проверьте, что `telegram_bot.py` запущен и работает
2. Проверьте логи:
   ```bash
   tail -f /path/to/Your_donor/website/backend/telegram_bot.log
   ```
3. Проверьте токен бота в .env файле
4. Убедитесь, что есть доступ к api.telegram.org

### Ошибка: "Port already in use"

**Решение:**
```bash
# Найдите процесс на порту 5001:
lsof -i :5001

# Завершите процесс:
kill -9 PID_ПРОЦЕССА
```

### Frontend не открывается

**Решение:**
1. Проверьте, что HTTP сервер запущен на порту 8080
2. Откройте в браузере точный адрес: `http://localhost:8080`
3. Проверьте консоль браузера (F12) на наличие ошибок

---

## Следующие шаги

После успешной установки:

1. **Ознакомьтесь с документацией:**
   - `README.md` - общее описание проекта
   - `REQUIREMENTS.md` - детальные требования
   - `ENV_SETUP_GUIDE.md` - подробности по .env файлу
   - `DEPLOYMENT.md` - деплой на продакшен

2. **Протестируйте функционал:**
   - Зарегистрируйте донора и медцентр
   - Создайте запрос крови
   - Откликнитесь на запрос
   - Проверьте мессенджер
   - Привяжите Telegram

3. **Настройте для продакшена:**
   - Получите домен
   - Настройте HTTPS (Let's Encrypt)
   - Смените пароли и SECRET_KEY
   - Настройте автозапуск (systemd)
   - Настройте резервное копирование БД

---

## Поддержка

При возникновении проблем:
1. Проверьте логи (flask.log, telegram_bot.log)
2. Проверьте консоль браузера (F12 → Console)
3. Просмотрите файлы документации
4. Убедитесь, что все зависимости установлены

**Полезные команды для диагностики:**
```bash
# Проверка версий
python3 --version
psql --version
pip3 list

# Проверка процессов
ps aux | grep python3
ps aux | grep postgres

# Проверка портов
lsof -i :5001
lsof -i :8080
lsof -i :5432

# Логи
tail -f website/backend/flask.log
tail -f website/backend/telegram_bot.log
```
