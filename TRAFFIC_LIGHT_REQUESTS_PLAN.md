# 🚦 ДЕТАЛЬНЫЙ ПЛАН: Отображение запросов из светофора

## 📋 ОГЛАВЛЕНИЕ
1. [Общая концепция](#общая-концепция)
2. [Архитектура решения](#архитектура-решения)
3. [Backend (уже готов)](#backend-уже-готов)
4. [Frontend изменения](#frontend-изменения)
5. [CSS стили](#css-стили)
6. [Поведение UI](#поведение-ui)
7. [Примеры карточек](#примеры-карточек)

---

## 🎯 Общая концепция

### Текущая ситуация:
```
Медцентр меняет светофор O+ с 🟢 NORMAL на 🟡 NEEDED
↓
✅ Backend создает blood_request (source='traffic_light')
✅ Отправляются уведомления донорам
❌ Запрос НЕ отображается в разделе "Запросы крови"
```

### Желаемая ситуация:
```
Медцентр меняет светофор O+ с 🟢 NORMAL на 🟡 NEEDED
↓
✅ Backend создает blood_request (source='traffic_light')
✅ Отправляются уведомления донорам
✅ Запрос ОТОБРАЖАЕТСЯ в разделе "Запросы крови"
✅ С бейджем "🚦 Из светофора"
✅ Нельзя редактировать вручную (только через светофор)
✅ При возврате светофора в 🟢 → запрос закрывается
```

---

## 🏗️ Архитектура решения

### Данные в БД:
```sql
blood_requests
  ├─ id
  ├─ blood_type: 'O+'
  ├─ urgency: 'needed' | 'urgent' | 'critical'
  ├─ status: 'active' | 'closed'
  ├─ source: 'traffic_light' | 'manual'  ← КЛЮЧЕВОЕ ПОЛЕ
  ├─ created_at
  └─ expires_at
```

### Поток данных:
```
[Медцентр] → Изменяет светофор
              ↓
[Backend] → PUT /api/blood-needs/<mc_id>
              ├─ Создает/обновляет blood_request
              ├─ source = 'traffic_light'
              └─ Отправляет уведомления
              ↓
[Backend] → GET /api/blood-requests
              └─ Возвращает ВСЕ запросы (включая source)
              ↓
[Frontend] → Отображает карточки
              ├─ Если source='traffic_light' → бейдж "🚦"
              ├─ Запрет редактирования
              └─ Особый стиль
```

---

## ✅ Backend (уже готов!)

### Миграция БД:
```sql
-- ✅ УЖЕ ВЫПОЛНЕНО
ALTER TABLE blood_requests 
ADD COLUMN IF NOT EXISTS source VARCHAR(20) DEFAULT 'manual';
```

### API Endpoint GET /api/blood-requests:
```python
# ✅ УЖЕ ВОЗВРАЩАЕТ source
query = """
    SELECT id, blood_type, urgency, status, description, source,  ← УЖЕ ЕСТЬ!
           created_at, expires_at, fulfilled_at,
           responses_count, approved_count
    FROM blood_requests
    WHERE medical_center_id = %s
"""
```

### Создание из светофора:
```python
# ✅ УЖЕ РЕАЛИЗОВАНО
if status == 'needed':
    request_id = query_db(
        """INSERT INTO blood_requests 
           (medical_center_id, blood_type, urgency, status, source)
           VALUES (%s, %s, 'needed', 'active', 'traffic_light')  ← УЖЕ ЕСТЬ!
           RETURNING id""",
        (mc_id, blood_type), commit=True, one=True
    )
```

**Вывод:** Backend полностью готов, ничего менять не нужно! ✅

---

## 🎨 Frontend изменения

### Файл: `website/js/medcenter-dashboard.js`

#### Изменение 1: Добавить бейдж источника в карточку

**Где:** Функция `renderBloodRequests`, строка ~987

**Было:**
```javascript
<header class="card-header">
    <div class="urgency-badge urgency-badge--${req.urgency}">
        <span class="urgency-dot"></span>
        <span class="urgency-text">${urgencyLabels[req.urgency]}</span>
    </div>
    <time class="card-time">${timeAgo}</time>
</header>
```

**Стало:**
```javascript
<header class="card-header">
    <div class="urgency-badge urgency-badge--${req.urgency}">
        <span class="urgency-dot"></span>
        <span class="urgency-text">${urgencyLabels[req.urgency]}</span>
    </div>
    
    <!-- НОВЫЙ БЕЙДЖ ИСТОЧНИКА -->
    ${req.source === 'traffic_light' ? `
        <div class="source-badge source-badge--traffic-light" title="Создано автоматически при изменении светофора">
            <span class="source-icon">🚦</span>
            <span class="source-text">Из светофора</span>
        </div>
    ` : ''}
    
    <time class="card-time">${timeAgo}</time>
</header>
```

#### Изменение 2: Скрыть кнопку "Редактировать" для запросов из светофора

**Где:** Функция `renderBloodRequests`, строка ~1032

**Было:**
```javascript
<button class="btn btn-ghost btn-sm" onclick="editRequest(${req.id})">
    Редактировать
</button>
```

**Стало:**
```javascript
${req.source !== 'traffic_light' ? `
    <button class="btn btn-ghost btn-sm" onclick="editRequest(${req.id})">
        Редактировать
    </button>
` : `
    <div class="info-hint" title="Этот запрос управляется через светофор">
        <span class="icon">🚦</span>
        <span class="text">Управление через светофор</span>
    </div>
`}
```

#### Изменение 3: Скрыть кнопку "✕ Отменить" для запросов из светофора

**Где:** Функция `renderBloodRequests`, строка ~1039

**Было:**
```javascript
<button class="btn btn-icon-only btn-ghost btn-sm" onclick="cancelRequest(${req.id})" title="Отменить">
    ✕
</button>
```

**Стало:**
```javascript
${req.source !== 'traffic_light' ? `
    <button class="btn btn-icon-only btn-ghost btn-sm" onclick="cancelRequest(${req.id})" title="Отменить">
        ✕
    </button>
` : ''}
```

#### Изменение 4: Автообновление списка при изменении светофора

**Где:** После успешного вызова светофора (нужно найти функцию обновления светофора)

**Добавить:**
```javascript
// После успешного обновления светофора
async function updateBloodStatus(bloodType, newStatus) {
    try {
        const response = await fetch(`${MC_API_URL}/blood-needs/${mcId}`, {
            method: 'PUT',
            headers: getAuthHeaders(),
            body: JSON.stringify({
                blood_type: bloodType,
                status: newStatus
            })
        });
        
        if (response.ok) {
            // ✨ НОВОЕ: Перезагружаем список запросов
            await loadBloodRequestsFromAPI();
            
            showNotification('Статус светофора обновлен', 'success');
        }
    } catch (error) {
        console.error('Ошибка обновления светофора:', error);
    }
}
```

---

## 🎨 CSS стили

### Файл: `website/css/blood-request-cards.css`

Добавить в конец файла:

```css
/* ═════════════════════════════════════════════════════
   Бейдж источника запроса (светофор / вручную)
   ═════════════════════════════════════════════════════ */

.source-badge {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    padding: 4px 10px;
    border-radius: 12px;
    font-size: 12px;
    font-weight: 500;
    line-height: 1;
    white-space: nowrap;
    transition: all 0.2s ease;
}

.source-badge--traffic-light {
    background: linear-gradient(135deg, #4CAF50 0%, #2E7D32 100%);
    color: white;
    box-shadow: 0 2px 4px rgba(76, 175, 80, 0.2);
}

.source-badge--traffic-light:hover {
    box-shadow: 0 4px 8px rgba(76, 175, 80, 0.3);
    transform: translateY(-1px);
}

.source-icon {
    font-size: 14px;
    line-height: 1;
}

.source-text {
    font-weight: 600;
}

/* Подсказка вместо кнопки редактирования */
.info-hint {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    padding: 8px 12px;
    background: #F5F5F5;
    border-radius: 8px;
    font-size: 13px;
    color: #757575;
    cursor: help;
    transition: all 0.2s ease;
}

.info-hint:hover {
    background: #EEEEEE;
}

.info-hint .icon {
    font-size: 16px;
}

.info-hint .text {
    font-weight: 500;
}

/* Адаптация шапки карточки для размещения бейджа */
.card-header {
    display: flex;
    align-items: center;
    gap: 12px;
    flex-wrap: wrap;
}

.card-header .urgency-badge {
    flex-shrink: 0;
}

.card-header .source-badge {
    flex-shrink: 0;
}

.card-header .card-time {
    margin-left: auto;
}

/* Мобильная адаптация */
@media (max-width: 640px) {
    .source-badge {
        font-size: 11px;
        padding: 3px 8px;
    }
    
    .source-text {
        display: none; /* На мобильных только иконка */
    }
    
    .info-hint .text {
        display: none; /* На мобильных только иконка */
    }
}
```

---

## 🎭 Поведение UI

### Сценарий 1: Создание запроса через светофор

```
1. Медцентр открывает раздел "Донорский светофор"
2. Меняет статус O+ с 🟢 на 🟡
   ↓
3. Backend создает blood_request (source='traffic_light')
   ↓
4. Frontend автоматически обновляет раздел "Запросы крови"
   ↓
5. Появляется новая карточка:
   ┌──────────────────────────────────┐
   │ 🟡 Нужна кровь  🚦 Из светофора │
   │                                  │
   │ O+                               │
   │ Откликнулось: 0                  │
   │                                  │
   │ [👥 Доноры] [🚦 Управление...]  │
   │ [Выполнен]                       │
   └──────────────────────────────────┘
```

### Сценарий 2: Попытка редактирования

```
1. Медцентр видит запрос с бейджем "🚦 Из светофора"
2. Вместо кнопки "Редактировать" видит подсказку:
   [🚦 Управление через светофор]
3. При наведении tooltip: 
   "Этот запрос управляется через светофор"
```

### Сценарий 3: Закрытие запроса

```
1. Медцентр возвращает светофор O+ в 🟢 NORMAL
   ↓
2. Backend закрывает blood_request (status='closed')
   ↓
3. Frontend автоматически обновляет список
   ↓
4. Карточка исчезает из раздела "Активные запросы"
5. Появляется в разделе "Закрытые" (если выбран фильтр)
```

### Сценарий 4: Повышение срочности

```
1. Текущий статус: 🟡 NEEDED
2. Медцентр меняет на 🟠 URGENT
   ↓
3. Backend обновляет urgency в том же запросе
   ↓
4. Frontend автоматически обновляет карточку:
   
   Было:                    Стало:
   🟡 Нужна кровь    →      🟠 Срочный
   🚦 Из светофора          🚦 Из светофора
```

---

## 📸 Примеры карточек

### Пример 1: Запрос из светофора (NEEDED)

```
┌────────────────────────────────────────────┐
│ 🟡 Нужна кровь    🚦 Из светофора    2ч   │
├────────────────────────────────────────────┤
│                                            │
│ O+                                         │
│                                            │
│ Истекает: 31 янв                          │
│                                            │
│ Откликнулось                               │
│ 3 из 10                                    │
│ [████████░░░░░░░░] 30%                    │
│                                            │
├────────────────────────────────────────────┤
│ [👥 Доноры (3)]  [🚦 Управление...]       │
│                  [Выполнен]                │
└────────────────────────────────────────────┘
```

### Пример 2: Запрос созданный вручную

```
┌────────────────────────────────────────────┐
│ 🔴 Критичный                          5ч   │
├────────────────────────────────────────────┤
│                                            │
│ AB-                                        │
│                                            │
│ Истекает: 25 янв                          │
│                                            │
│ Откликнулось                               │
│ 5                                          │
│                                            │
├────────────────────────────────────────────┤
│ [👥 Доноры (5)]  [Редактировать]         │
│                  [Выполнен]  [✕]          │
└────────────────────────────────────────────┘
```

### Пример 3: Запрос из светофора (URGENT)

```
┌────────────────────────────────────────────┐
│ 🟠 Срочный     🚦 Из светофора       1д   │
├────────────────────────────────────────────┤
│                                            │
│ A+                                         │
│                                            │
│ Истекает: 27 янв                          │
│                                            │
│ Откликнулось                               │
│ 12                                         │
│                                            │
├────────────────────────────────────────────┤
│ [👥 Доноры (12)]  [🚦 Управление...]      │
│                   [Выполнен]               │
└────────────────────────────────────────────┘
```

---

## 🔄 Синхронизация светофора и запросов

### Двусторонняя связь:

```
СВЕТОФОР                        ЗАПРОСЫ КРОВИ
────────                        ──────────────

🟢 NORMAL    ←─────────────→    Нет активных запросов

🟡 NEEDED    ←─────────────→    ┌──────────────────┐
                                │ 🟡 Нужна кровь   │
                                │ 🚦 Из светофора  │
                                └──────────────────┘

🟠 URGENT    ←─────────────→    ┌──────────────────┐
                                │ 🟠 Срочный       │
                                │ 🚦 Из светофора  │
                                └──────────────────┘

🔴 CRITICAL  ←─────────────→    ┌──────────────────┐
                                │ 🔴 Критичный     │
                                │ 🚦 Из светофора  │
                                └──────────────────┘
```

### При возврате в норму:

```
Было:
🟠 URGENT → Есть активный запрос в списке

Медцентр меняет на 🟢 NORMAL
↓
Backend закрывает запрос (status='closed')
↓
Frontend обновляет список
↓
Стало:
🟢 NORMAL → Запрос исчез из "Активных"
```

---

## ✅ Чеклист реализации

### Backend:
- [x] Поле `source` добавлено в БД
- [x] При создании из светофора `source='traffic_light'`
- [x] API возвращает поле `source`
- [x] При возврате в NORMAL запрос закрывается

### Frontend JavaScript:
- [ ] Добавить бейдж "🚦 Из светофора" в карточку
- [ ] Скрыть кнопку "Редактировать" для запросов из светофора
- [ ] Скрыть кнопку "✕ Отменить" для запросов из светофора
- [ ] Автообновление списка после изменения светофора

### Frontend CSS:
- [ ] Стили для `.source-badge`
- [ ] Стили для `.info-hint`
- [ ] Адаптация `.card-header`
- [ ] Мобильная адаптация

### Тестирование:
- [ ] Создание запроса через светофор → появляется в списке
- [ ] Запрос имеет бейдж "🚦 Из светофора"
- [ ] Нельзя редактировать
- [ ] Возврат светофора в норму → запрос закрывается
- [ ] Повышение срочности → карточка обновляется

---

## 📝 Итоговый результат

После реализации медцентр увидит:

1. ✅ Все запросы (ручные + из светофора) в одном месте
2. ✅ Ясное визуальное различие (бейдж 🚦)
3. ✅ Нельзя случайно испортить запрос из светофора
4. ✅ Синхронизация: светофор ↔ список запросов
5. ✅ Полная информация: отклики, прогресс, доноры

**Пользовательский опыт станет понятным и логичным!** 🎯✨
