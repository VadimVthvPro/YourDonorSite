# 🎯 РУЧНОЕ ИСПРАВЛЕНИЕ МОБИЛЬНОГО CSS

## ⚠️ ПРОБЛЕМА: Противопоказания всё ещё вертикальные

Нужно вручную загрузить исправленный CSS на сервер.

---

## 📋 ПОШАГОВАЯ ИНСТРУКЦИЯ:

### Шаг 1: Загрузить CSS на сервер

```bash
scp /Users/VadimVthv/Your_donor/website/css/styles.css root@178.172.212.221:/opt/tvoydonor/website/css/styles.css
```

**Пароль:** `Vadamahjkl1!`

---

### Шаг 2: Подключиться к серверу

```bash
ssh root@178.172.212.221
```

**Пароль:** `Vadamahjkl1!`

---

### Шаг 3: Обновить версию CSS в HTML (на сервере)

Скопируйте и вставьте **все команды целиком**:

```bash
# Генерируем новую версию (timestamp)
TIMESTAMP=$(date +%s)

# Обновляем index.html
sed -i "s|styles\.css?v=[0-9]*|styles.css?v=$TIMESTAMP|g" /opt/tvoydonor/website/index.html

# Обновляем donor-dashboard.html
sed -i "s|styles\.css?v=[0-9]*|styles.css?v=$TIMESTAMP|g" /opt/tvoydonor/website/donor-dashboard.html

# Обновляем medcenter-dashboard.html
sed -i "s|styles\.css?v=[0-9]*|styles.css?v=$TIMESTAMP|g" /opt/tvoydonor/website/medcenter-dashboard.html

# Проверяем что обновилось
echo "Новая версия CSS: $TIMESTAMP"
grep "styles.css" /opt/tvoydonor/website/index.html | head -1
```

---

### Шаг 4: Перезагрузить nginx (на сервере)

```bash
nginx -t && systemctl reload nginx
```

Должно показать:
```
nginx: the configuration file /etc/nginx/nginx.conf syntax is ok
nginx: configuration file /etc/nginx/nginx.conf test is successful
```

---

### Шаг 5: Проверить что CSS обновился (на сервере)

```bash
grep -A 10 "@media (max-width: 768px)" /opt/tvoydonor/website/css/styles.css | grep -A 5 ".contra-card {"
```

Должно показать:
```css
.contra-card {
    display: flex;
    flex-direction: row;  /* <-- ГЛАВНОЕ! */
    align-items: center;
    gap: 12px;
```

---

### Шаг 6: Выйти с сервера

```bash
exit
```

---

## 🧪 ТЕСТИРОВАНИЕ НА ТЕЛЕФОНЕ:

1. Откройте `https://tvoydonor.by`
2. **ЖЁСТКО обновите страницу:**
   - **iOS/Mac:** `Cmd + Shift + R`
   - **Android:** `Ctrl + Shift + R`
   - Или: Настройки → Очистить кэш
3. Прокрутите до раздела **"Противопоказания"**
4. ✅ Каждая карточка должна быть **ГОРИЗОНТАЛЬНОЙ**:

```
[🔴 ИКОНКА] Постоянные противопоказания  • ВИЧ • Онкология • Болезни крови
            Донорство невозможно         • Сердечно-сосудистые • И т.д.
```

---

## 🔧 ЕСЛИ ВСЁ ЕЩЁ ВЕРТИКАЛЬНЫЕ:

### Вариант 1: Проверить что CSS загрузился
```bash
ssh root@178.172.212.221
tail -50 /opt/tvoydonor/website/css/styles.css
```

Должна быть секция `@media (max-width: 768px)` с `.contra-card { display: flex; flex-direction: row; }`

### Вариант 2: Проверить версию в браузере
1. Откройте https://tvoydonor.by
2. F12 (Developer Tools)
3. Network → Фильтр CSS
4. Найдите `styles.css?v=XXXXXX`
5. Номер версии должен совпадать с `$TIMESTAMP` из Шага 3

### Вариант 3: Жёсткая перезагрузка nginx
```bash
ssh root@178.172.212.221
systemctl restart nginx
```

---

## 📸 КАК ДОЛЖНО ВЫГЛЯДЕТЬ:

### ❌ НЕПРАВИЛЬНО (вертикально):
```
┌──────────────────┐
│ [ИКОНКА]         │
│ Постоянные       │
│ противопоказания │
│──────────────────│
│ • ВИЧ-инфекция   │
│ • Онкология      │
│ • Болезни крови  │
└──────────────────┘
```

### ✅ ПРАВИЛЬНО (горизонтально):
```
┌────────────────────────────────────────────────┐
│ [ИКОНКА] Постоянные  • ВИЧ • Онкология        │
│          противопоказания • Болезни крови      │
└────────────────────────────────────────────────┘
```

---

## 🆘 ЕСЛИ НИЧЕГО НЕ ПОМОГЛО:

Отправьте скриншот того, что видите на телефоне, и я создам ДРУГОЙ подход к решению!

---

**НАЧНИТЕ С ШАГА 1!** 🚀
