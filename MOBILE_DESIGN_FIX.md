# 🎨 ИСПРАВЛЕНИЯ МОБИЛЬНОГО ДИЗАЙНА

## ❌ ПРОБЛЕМЫ:
1. **Противопоказания** не вмещаются в вертикальные колонны на мобильных
2. **Таблица интервалов** не вмещается на экран телефона

## ✅ РЕШЕНИЯ:

### 1. Противопоказания
- **УЖЕ ИСПРАВЛЕНО** в существующих стилях!
- На мобильных (`max-width: 768px`) карточки автоматически становятся горизонтальными (одна под другой)
- `grid-template-columns: 1fr` вместо `repeat(2, 1fr)`
- Убран hover-эффект на тач-устройствах (не нужен)

### 2. Таблица интервалов между донациями
- **ДОБАВЛЕНА** горизонтальная прокрутка для мобильных
- Таблица прокручивается влево-вправо (swipe)
- Минимальная ширина: 600px (можно прокручивать)
- **ДОБАВЛЕН** визуальный индикатор "← Прокрутите →" в правом нижнем углу
- Плавная прокрутка с `-webkit-overflow-scrolling: touch`
- Пульсирующая анимация индикатора для привлечения внимания

## 📊 ИЗМЕНЁННЫЕ ФАЙЛЫ:
- `website/css/styles.css` — добавлены адаптивные стили для таблицы

## 🔧 КАК ПРИМЕНИТЬ:
```bash
cd /Users/VadimVthv/Your_donor
./fix_mobile_design.sh
```

## 🧪 ТЕСТИРОВАНИЕ:
1. Откройте https://tvoydonor.by на телефоне
2. Прокрутите до "Противопоказания" → карточки должны быть горизонтальными
3. Прокрутите до "Интервалы между донациями" → таблица прокручивается влево-вправо
4. Должна быть видна подсказка "← Прокрутите →"

## 📱 АЛЬТЕРНАТИВА (DevTools):
- F12 → Toggle Device Toolbar (Ctrl+Shift+M)
- Выберите iPhone 12 Pro или Galaxy S20
- Проверьте те же разделы
