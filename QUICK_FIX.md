# ⚡ БЫСТРАЯ ШПАРГАЛКА: 7 КОМАНД

## 🎯 ЦЕЛЬ:
Сделать противопоказания **читаемыми** на мобильных:
- Плашки на всю ширину (1 колонка)
- Текст крупнее на 15-20%

---

## 📋 КОМАНДЫ (копировать по порядку):

### 1️⃣ Резервная копия
```bash
ssh root@178.172.212.221
```
Пароль: `Vadamahjkl1!`

```bash
mkdir -p /opt/tvoydonor/backups && cp /opt/tvoydonor/website/css/styles.css /opt/tvoydonor/backups/styles.css.backup.$(date +%Y%m%d_%H%M%S) && exit
```

### 2️⃣ Загрузить CSS
```bash
scp /Users/VadimVthv/Your_donor/website/css/styles.css root@178.172.212.221:/opt/tvoydonor/website/css/styles.css
```
Пароль: `Vadamahjkl1!`

### 3️⃣ Проверить + обновить версию
```bash
ssh root@178.172.212.221
```
Пароль: `Vadamahjkl1!`

```bash
grep -c "grid-template-columns: 1fr; /\* ОДНА КОЛОНКА" /opt/tvoydonor/website/css/styles.css && grep -c "font-size: 0.95rem; /\* Увеличен" /opt/tvoydonor/website/css/styles.css
```
Должно показать: `1` и `1`

### 4️⃣ Обновить версию CSS
```bash
TIMESTAMP=$(date +%s) && sed -i "s|styles\.css?v=[0-9]*|styles.css?v=$TIMESTAMP|g" /opt/tvoydonor/website/index.html && sed -i "s|href=\"css/styles\.css\"|href=\"css/styles.css?v=$TIMESTAMP\"|g" /opt/tvoydonor/website/index.html && echo "Версия: $TIMESTAMP" && grep "styles.css" /opt/tvoydonor/website/index.html | head -1
```

### 5️⃣ Перезагрузить nginx
```bash
nginx -t && systemctl reload nginx
```

### 6️⃣ Проверить
```bash
echo "1. grid:" && grep -A 8 "@media (max-width: 768px)" /opt/tvoydonor/website/css/styles.css | grep "grid-template-columns:" | head -1 && echo "2. font:" && grep -A 50 "@media (max-width: 768px)" /opt/tvoydonor/website/css/styles.css | grep ".contra-title" -A 4 | grep "font-size" | head -1
```

Должно показать:
```
1. grid:
        grid-template-columns: 1fr; /* ОДНА КОЛОНКА */
2. font:
        font-size: 0.95rem; /* Увеличен */
```

### 7️⃣ Выйти
```bash
exit
```

---

## 🧪 ТЕСТ НА ТЕЛЕФОНЕ:

1. **Очистить кэш:**
   - iOS: Настройки → Safari → Очистить историю
   - Android: Chrome → ⋮ → Очистить данные браузера → Кэш

2. **Открыть:** `https://tvoydonor.by`

3. **Проверить:** Раздел "Противопоказания"

**✅ Должно быть:**
```
┌───────────────────────────────┐
│ 🔴 Постоянные противопоказания│
│    Донорство невозможно       │
│    • ВИЧ • Онкология...      │
└───────────────────────────────┘
┌───────────────────────────────┐
│ 🟡 Временные противопоказания │
│    На определённый срок       │
│    • Грипп • ОРВИ...         │
└───────────────────────────────┘
```

- Плашки **на всю ширину**
- Текст **крупный**
- **Легко читать**

---

## 🆘 ЕСЛИ НЕ РАБОТАЕТ:

### Вариант 1: Режим инкогнито
Откройте сайт в **приватном режиме** (там нет кэша).

### Вариант 2: Жёсткая очистка nginx
```bash
ssh root@178.172.212.221
systemctl restart nginx
exit
```

### Вариант 3: Новый timestamp
```bash
ssh root@178.172.212.221
RANDOM=$(shuf -i 10000000-99999999 -n 1) && sed -i "s|styles\.css?v=[0-9]*|styles.css?v=$RANDOM|g" /opt/tvoydonor/website/index.html && systemctl reload nginx && echo "Версия: $RANDOM"
exit
```

---

## 📊 ЧТО ИЗМЕНИЛОСЬ:

| Элемент | Было | Стало |
|---------|------|-------|
| Колонки | 2 | **1** |
| Заголовок | 0.85rem | **0.95rem** |
| Список | 0.75rem | **0.85rem** |
| Иконка | 36px | **40px** |

---

**НАЧНИТЕ С КОМАНДЫ 1️⃣** и выполните по порядку!
