# 🎯 КОМПЛЕКСНАЯ ИНСТРУКЦИЯ: ИСПРАВЛЕНИЕ МОБИЛЬНОГО CSS

## ✅ ПРОВЕРКА ЛОКАЛЬНОГО CSS ЗАВЕРШЕНА:
- Размер: 70828 байт, 3338 строк
- ✅ Найдено: `grid-template-columns: 1fr` (плашки на всю ширину)
- ✅ Найдено: `font-size: 0.95rem` (увеличенный шрифт)

**Локальный CSS готов к загрузке!**

---

## 📋 ПОШАГОВАЯ ИНСТРУКЦИЯ (7 ШАГОВ):

### ШАГ 1: Создать резервную копию на сервере

```bash
ssh root@178.172.212.221
```
**Пароль:** `Vadamahjkl1!`

После подключения выполните:

```bash
mkdir -p /opt/tvoydonor/backups
cp /opt/tvoydonor/website/css/styles.css /opt/tvoydonor/backups/styles.css.backup.$(date +%Y%m%d_%H%M%S)
echo "✅ Резервная копия создана"
ls -lh /opt/tvoydonor/backups/
exit
```

---

### ШАГ 2: Загрузить CSS на сервер

```bash
scp /Users/VadimVthv/Your_donor/website/css/styles.css root@178.172.212.221:/opt/tvoydonor/website/css/styles.css
```
**Пароль:** `Vadamahjkl1!`

---

### ШАГ 3: Проверить что CSS загрузился

```bash
ssh root@178.172.212.221
```
**Пароль:** `Vadamahjkl1!`

Выполните проверку:

```bash
echo "Размер файла на сервере:"
ls -lh /opt/tvoydonor/website/css/styles.css

echo ""
echo "Проверка изменений:"
grep -c "grid-template-columns: 1fr; /\* ОДНА КОЛОНКА" /opt/tvoydonor/website/css/styles.css
grep -c "font-size: 0.95rem; /\* Увеличен" /opt/tvoydonor/website/css/styles.css
```

Должно показать:
```
1
1
```

Если показывает `0` - значит файл не обновился, повторите ШАГ 2!

---

### ШАГ 4: Обновить версию CSS в HTML

**НЕ ВЫХОДИТЕ** с сервера, выполните:

```bash
TIMESTAMP=$(date +%s)
echo "Новая версия CSS: $TIMESTAMP"

# Обновляем index.html
sed -i "s|styles\.css?v=[0-9]*|styles.css?v=$TIMESTAMP|g" /opt/tvoydonor/website/index.html
sed -i "s|href=\"css/styles\.css\"|href=\"css/styles.css?v=$TIMESTAMP\"|g" /opt/tvoydonor/website/index.html

# Обновляем donor-dashboard.html (если есть)
if [ -f /opt/tvoydonor/website/donor-dashboard.html ]; then
    sed -i "s|styles\.css?v=[0-9]*|styles.css?v=$TIMESTAMP|g" /opt/tvoydonor/website/donor-dashboard.html
fi

# Обновляем medcenter-dashboard.html (если есть)
if [ -f /opt/tvoydonor/website/medcenter-dashboard.html ]; then
    sed -i "s|styles\.css?v=[0-9]*|styles.css?v=$TIMESTAMP|g" /opt/tvoydonor/website/medcenter-dashboard.html
fi

echo "Проверяем что версия обновилась:"
grep "styles.css" /opt/tvoydonor/website/index.html | head -1
```

Должно показать:
```
<link rel="stylesheet" href="css/styles.css?v=XXXXXXXXXX">
```

Где `XXXXXXXXXX` - это timestamp.

---

### ШАГ 5: Очистить кэш nginx и перезагрузить

**НЕ ВЫХОДИТЕ** с сервера, выполните:

```bash
# Проверяем конфигурацию
nginx -t

# Очищаем кэш (если есть)
if [ -d "/var/cache/nginx" ]; then
    rm -rf /var/cache/nginx/*
    echo "✅ Кэш nginx очищен"
fi

# Перезагружаем nginx
systemctl reload nginx
echo "✅ Nginx перезагружен"
```

Должно показать:
```
nginx: the configuration file /etc/nginx/nginx.conf syntax is ok
nginx: configuration file /etc/nginx/nginx.conf test is successful
```

---

### ШАГ 6: Финальная проверка на сервере

**НЕ ВЫХОДИТЕ** с сервера, выполните все команды:

```bash
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "ФИНАЛЬНАЯ ПРОВЕРКА:"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

echo ""
echo "1. grid-template-columns (должно быть: 1fr):"
grep -A 8 "@media (max-width: 768px)" /opt/tvoydonor/website/css/styles.css | grep "grid-template-columns:" | head -1

echo ""
echo "2. .contra-title font-size (должно быть: 0.95rem):"
grep -A 50 "@media (max-width: 768px)" /opt/tvoydonor/website/css/styles.css | grep ".contra-title" -A 4 | grep "font-size" | head -1

echo ""
echo "3. .contra-list li font-size (должно быть: 0.85rem):"
grep -A 80 "@media (max-width: 768px)" /opt/tvoydonor/website/css/styles.css | grep ".contra-list li {" -A 7 | grep "font-size"

echo ""
echo "4. Версия CSS в HTML:"
grep "styles.css" /opt/tvoydonor/website/index.html | head -1

echo ""
echo "5. Последнее изменение CSS файла:"
stat /opt/tvoydonor/website/css/styles.css | grep Modify

echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
```

**Ожидаемый вывод:**
```
1. grid-template-columns (должно быть: 1fr):
        grid-template-columns: 1fr; /* ОДНА КОЛОНКА - плашки на всю ширину */

2. .contra-title font-size (должно быть: 0.95rem):
        font-size: 0.95rem; /* Увеличен с 0.85rem */

3. .contra-list li font-size (должно быть: 0.85rem):
        font-size: 0.85rem; /* Увеличен с 0.75rem */

4. Версия CSS в HTML:
    <link rel="stylesheet" href="css/styles.css?v=1769364XXX">

5. Последнее изменение CSS файла:
Modify: 2026-01-25 XX:XX:XX.XXXXXXXXX +0300
```

---

### ШАГ 7: Выйти с сервера

```bash
exit
```

---

## 🧪 ТЕСТИРОВАНИЕ НА ТЕЛЕФОНЕ:

### 1. Откройте браузер на телефоне

Перейдите на `https://tvoydonor.by`

### 2. ЖЁСТКО ОБНОВИТЕ СТРАНИЦУ

**ЭТО КРИТИЧЕСКИ ВАЖНО!**

- **iOS (Safari):**
  1. Нажмите на значок "Обновить" в адресной строке
  2. **ИЛИ:** Настройки → Safari → Очистить историю и данные → Очистить
  3. Снова откройте `https://tvoydonor.by`

- **Android (Chrome):**
  1. Нажмите на три точки (⋮) → Настройки
  2. Конфиденциальность → Очистить данные браузера
  3. Выберите "Кэшированные изображения и файлы"
  4. Нажмите "Удалить данные"
  5. Снова откройте `https://tvoydonor.by`

### 3. Прокрутите до "Противопоказания"

Найдите раздел "Противопоказания" на главной странице.

### 4. Проверьте результат

**✅ ПРАВИЛЬНО (как должно быть):**

```
┌──────────────────────────────────────────────────┐
│ [🔴] Постоянные противопоказания                │
│      Донорство невозможно                       │
│                                                  │
│      • ВИЧ-инфекция, гепатиты B и C             │
│      • Онкологические заболевания               │
│      • Заболевания крови                        │
│      • Сердечно-сосудистые заболевания          │
└──────────────────────────────────────────────────┘
┌──────────────────────────────────────────────────┐
│ [🟡] Временные противопоказания                 │
│      На определённый срок                       │
│                                                  │
│      • ОРВИ, грипп [1 месяц]                    │
│      • Удаление зуба [10 дней]                  │
│      • Прививки [10 дн — 1 год]                 │
└──────────────────────────────────────────────────┘
```

**Характеристики:**
- ✅ Плашки **НА ВСЮ ШИРИНУ** экрана (одна под другой)
- ✅ Текст внутри **ГОРИЗОНТАЛЬНО**: `[иконка] [заголовок] [список]`
- ✅ Шрифт **КРУПНЫЙ И ЧИТАЕМЫЙ**
- ✅ Между элементами **ДОСТАТОЧНО ВОЗДУХА**

---

**❌ НЕПРАВИЛЬНО (если видите):**

```
┌────────┬────────┐
│ Плашка │ Плашка │  ← Две узкие колонки
│   1    │   2    │
└────────┴────────┘
```

Или текст мелкий и нечитаемый → **Кэш браузера не очищен!**

---

## 🔍 ДОПОЛНИТЕЛЬНАЯ ПРОВЕРКА (если не работает):

### Проверка 1: Версия CSS в браузере

1. Откройте `https://tvoydonor.by` на телефоне
2. Если есть DevTools (F12 на десктопе):
   - Network → Фильтр CSS
   - Найдите `styles.css?v=XXXXXXXXXX`
   - Номер версии должен совпадать с тем, что в ШАГе 6 пункт 4

### Проверка 2: Computed styles

1. DevTools → Elements
2. Найдите `<div class="contra-grid">`
3. Computed → `grid-template-columns`
4. Должно быть: **одно значение** (например `542px`), а не два (например `271px 271px`)

### Проверка 3: Режим инкогнито

Откройте сайт в **режиме инкогнито** (приватный просмотр) - там кэша нет совсем.

---

## 🆘 ЕСЛИ НИЧЕГО НЕ ПОМОГЛО:

### Вариант 1: Жёсткая очистка на сервере

```bash
ssh root@178.172.212.221

# Полная перезагрузка nginx
systemctl stop nginx
sleep 2
systemctl start nginx

# Проверка
systemctl status nginx

exit
```

### Вариант 2: Добавить случайный параметр

```bash
ssh root@178.172.212.221

RANDOM_VERSION=$(shuf -i 10000000-99999999 -n 1)
sed -i "s|styles\.css?v=[0-9]*|styles.css?v=$RANDOM_VERSION|g" /opt/tvoydonor/website/index.html
echo "Новая версия: $RANDOM_VERSION"
systemctl reload nginx

exit
```

Потом снова очистите кэш браузера и проверьте.

---

## 📊 ТЕХНИЧЕСКИЕ ДЕТАЛИ ИЗМЕНЕНИЙ:

| Элемент | Было | Стало |
|---------|------|-------|
| `.contra-grid` columns | `repeat(2, 1fr)` | `1fr` ✅ |
| `.contra-title` font-size | `0.85rem` | `0.95rem` ✅ |
| `.contra-subtitle` font-size | `0.7rem` | `0.75rem` ✅ |
| `.contra-list li` font-size | `0.75rem` | `0.85rem` ✅ |
| `.contra-icon-wrapper` size | `36x36px` | `40x40px` ✅ |
| `.contra-card` padding | `12px 14px` | `14px 16px` ✅ |

---

## ✅ ЧЕКЛИСТ ВЫПОЛНЕНИЯ:

- [ ] ШАГ 1: Создана резервная копия на сервере
- [ ] ШАГ 2: CSS загружен на сервер
- [ ] ШАГ 3: Проверено что CSS обновился (показывает `1` и `1`)
- [ ] ШАГ 4: Версия CSS обновлена в HTML (показывает новый timestamp)
- [ ] ШАГ 5: Nginx перезагружен (показывает `syntax is ok`)
- [ ] ШАГ 6: Финальная проверка прошла (все 5 пунктов корректны)
- [ ] ШАГ 7: Вышли с сервера
- [ ] ТЕСТ: Очищен кэш браузера на телефоне
- [ ] ТЕСТ: Плашки на всю ширину, текст читаемый

---

**НАЧНИТЕ С ШАГА 1 И ПОСЛЕДОВАТЕЛЬНО ВЫПОЛНИТЕ ВСЕ ШАГИ!**

После завершения напишите что получилось на телефоне.

Если что-то пошло не так на каком-то шаге - напишите номер шага и текст ошибки.
