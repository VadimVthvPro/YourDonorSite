# 🔐 ИНСТРУКЦИЯ ПО НАСТРОЙКЕ .env ФАЙЛА

## ✅ ЧТО УЖЕ СДЕЛАНО

Все секретные данные теперь загружаются из `.env` файла через переменные окружения:

### Файлы, которые используют .env:
- `/website/backend/app.py` - основной API сервер
- `/website/backend/telegram_bot.py` - Telegram бот
- `/website/backend/messaging_api.py` - API мессенджера
- `/website/backend/messaging_api_messages.py` - API сообщений

---

## 📋 КАКИЕ ПЕРЕМЕННЫЕ НУЖНО ЗАПОЛНИТЬ

### 1. **БАЗА ДАННЫХ** (обязательно!)

```env
DB_HOST=localhost          # Хост БД
DB_PORT=5432              # Порт PostgreSQL
DB_NAME=your_donor        # Название вашей базы данных
DB_USER=postgres          # Пользователь БД
DB_PASSWORD=ВАШ_ПАРОЛЬ    # ⚠️ ЗАМЕНИТЕ НА РЕАЛЬНЫЙ ПАРОЛЬ!
```

### 2. **БЕЗОПАСНОСТЬ** (обязательно!)

```env
# Секретный ключ Flask (сгенерируйте новый!)
SECRET_KEY=ВАШ_СЕКРЕТНЫЙ_КЛЮЧ

# Сгенерировать можно так:
# python3 -c "import secrets; print(secrets.token_hex(32))"

# Мастер-пароль для медцентров
MASTER_PASSWORD=ВАШ_МАСТЕР_ПАРОЛЬ
```

### 3. **TELEGRAM BOT** (обязательно!)

```env
TELEGRAM_BOT_TOKEN=ВАШ_ТОКЕН_БОТА
```

**Где взять токен:**
1. Откройте Telegram
2. Найдите @BotFather
3. Напишите `/newbot`
4. Следуйте инструкциям
5. Скопируйте токен (формат: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

**⚠️ ВАЖНО:** Вы уже указали токен: `8212814214:AAG29mEQN2EWS1wFvKbDqC8nr6SgN3_VeZ8`

### 4. **ДОМЕНЫ И URLs** (важно!)

```env
# URL вашего сайта (для WebApp и уведомлений)
# ⚠️ ДЛЯ TELEGRAM WEBAPP ОБЯЗАТЕЛЕН HTTPS!
WEBSITE_URL=https://tvoydonor.by

# URL API (для внутренних запросов)
APP_URL=http://localhost:8080
```

**Что вы сейчас используете:**
- `WEBSITE_URL=https://lmarena.ai/` ← Это правильно для продакшена!

### 5. **СЕРВЕР**

```env
FLASK_DEBUG=false    # В продакшене ОБЯЗАТЕЛЬНО false!
PORT=5001           # Порт Flask сервера
```

---

## 📝 ВАШ ТЕКУЩИЙ .env ФАЙЛ

Файл находится: `/Users/VadimVthv/Your_donor/website/backend/.env`

**Что нужно изменить:**

### ✅ УЖЕ ПРАВИЛЬНО:
- `TELEGRAM_BOT_TOKEN` - уже обновлен на правильный
- `WEBSITE_URL` - уже указан на `https://lmarena.ai/`

### ⚠️ НУЖНО ПРОВЕРИТЬ/ИЗМЕНИТЬ:

1. **DB_PASSWORD** - сейчас стоит `vadamahjkl`
   - Если это НЕ ваш реальный пароль БД, замените!
   
2. **DB_NAME** - сейчас стоит `your_donor`
   - Если ваша база данных называется иначе, замените!
   
3. **SECRET_KEY** - сейчас стоит `your-donor-secret-2026`
   - Рекомендуется сгенерировать более безопасный ключ
   
4. **MASTER_PASSWORD** - сейчас стоит `doctor2024`
   - Это пароль для первого входа медцентров
   
5. **FLASK_DEBUG** - сейчас стоит `true`
   - В продакшене измените на `false`!

---

## 🔧 КАК ОБНОВИТЬ .env

1. Откройте файл:
   ```bash
   nano /Users/VadimVthv/Your_donor/website/backend/.env
   ```

2. Измените нужные значения

3. Сохраните (Ctrl+O, Enter, Ctrl+X)

4. **Перезапустите сервисы:**
   ```bash
   # Остановить бота
   pkill -f telegram_bot.py
   
   # Остановить Flask
   lsof -ti:5001 | xargs kill -9
   
   # Запустить заново
   cd /Users/VadimVthv/Your_donor/website/backend
   python3 app.py &
   python3 telegram_bot.py &
   ```

---

## 📂 ПРИМЕР ФАЙЛА

Файл `env_example.txt` обновлен и содержит все необходимые переменные с комментариями.

**Где найти:**
- `/Users/VadimVthv/Your_donor/env_example.txt`
- `/Users/VadimVthv/Your_donor/website/backend/env_example.txt`

---

## ⚠️ БЕЗОПАСНОСТЬ

### НЕ ДЕЛАЙТЕ ЭТО:
- ❌ Не коммитьте `.env` в Git (он в `.gitignore`)
- ❌ Не публикуйте токены и пароли
- ❌ Не используйте слабые пароли
- ❌ Не оставляйте `FLASK_DEBUG=true` в продакшене

### ДЕЛАЙТЕ ЭТО:
- ✅ Генерируйте сложные SECRET_KEY
- ✅ Используйте уникальные пароли
- ✅ Храните `.env` файл в безопасности
- ✅ Регулярно меняйте пароли

---

## 🎯 ИТОГО: ЧТО ВАМ НУЖНО СДЕЛАТЬ

1. ✅ **TELEGRAM_BOT_TOKEN** - уже правильный
2. ⚠️ **DB_PASSWORD** - проверьте, что это ваш реальный пароль PostgreSQL
3. ⚠️ **DB_NAME** - проверьте название вашей базы данных
4. ⚠️ **SECRET_KEY** - сгенерируйте более безопасный ключ
5. ⚠️ **FLASK_DEBUG** - измените на `false` для продакшена
6. ✅ **WEBSITE_URL** - уже правильный (`https://lmarena.ai/`)

После изменений перезапустите сервисы!

---

## 📞 ПОМОЩЬ

Если что-то не работает:
1. Проверьте логи Flask: `tail -f /Users/VadimVthv/Your_donor/website/backend/flask.log`
2. Проверьте логи бота: `tail -f /Users/VadimVthv/Your_donor/website/backend/telegram_bot.log`
3. Проверьте, что БД доступна: `psql -U postgres -d your_donor`
