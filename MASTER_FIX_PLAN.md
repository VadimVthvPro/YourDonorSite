# 🎯 МАСТЕР-ПЛАН ИСПРАВЛЕНИЯ ВСЕХ ПРОБЛЕМ

**Дата:** 23.01.2026  
**Статус:** В разработке

---

## 📋 СПИСОК ПРОБЛЕМ (7 КРИТИЧЕСКИХ)

### 1. 🎨 ДИЗАЙН: Бейдж "Срочно" съехал и не оранжевый
- **Где:** Карточки запросов крови (донор + медцентр)
- **Проблема:** Бейдж должен быть оранжевым с рамкой, но отображается неправильно
- **Приоритет:** 🟡 СРЕДНИЙ
- **Файлы:** `request-cards-v2.css`, `donor-dashboard.js`, `medcenter-dashboard.js`

### 2. 📊 МАСШТАБИРУЕМОСТЬ: 100-10000 откликов
- **Где:** Меню медцентра → Запросы крови
- **Проблема:** Если откликов 100+, как их отображать? Как искать?
- **Приоритет:** 🔴 ВЫСОКИЙ
- **Решение:** Пагинация + поиск + фильтры

### 3. 📐 ОБРЕЗАННАЯ ИНФОРМАЦИЯ: Контент за пределами экрана
- **Где:** Меню медцентра → Запросы крови
- **Проблема:** Прямоугольные рамки слишком длинные, информация не видна
- **Приоритет:** 🔴 ВЫСОКИЙ
- **Решение:** Адаптивная верстка + прокрутка

### 4. 🖼️ МОДАЛ СОЗДАНИЯ ЗАПРОСА: Не вмещается на экран
- **Где:** Меню медцентра → Создать запрос крови
- **Проблема:** Модальное окно слишком большое
- **Приоритет:** 🟡 СРЕДНИЙ
- **Решение:** Уменьшить размеры + добавить прокрутку

### 5. 💬 ПЕРЕПИСКА: Нет функционала
- **Где:** Глобально
- **Проблема:** Переписка между донором и медцентром "в разработке"
- **Приоритет:** 🔴 КРИТИЧЕСКИЙ
- **Решение:** Полноценный чат с базой данных

### 6. 🚫 ОШИБКА 403: При изменении светофора
- **Где:** Меню медцентра → Светофор групп крови
- **Проблема:** "Доступ запрещён. Требуется роль: medcenter"
- **Приоритет:** 🔴 КРИТИЧЕСКИЙ
- **Файлы:** `app.py` (require_auth decorator)

### 7. 📄 ЗАГРУЗКА СПРАВКИ: Не работает
- **Где:** Донор → Профиль → Мед.справка
- **Проблема:** Нельзя загрузить и просмотреть справку
- **Приоритет:** 🔴 КРИТИЧЕСКИЙ
- **Файлы:** `donor-dashboard.js`, `app.py` (upload/view endpoints)

---

## 🔧 ПЛАН РЕШЕНИЯ

### ЭТАП 1: КРИТИЧЕСКИЕ ОШИБКИ (403 и загрузка справки)

#### 1.1 Исправить 403 для медцентра
```python
# app.py - require_auth decorator
# Проблема: session не содержит правильный user_type для медцентра
# Решение: Проверить логику сохранения сессии при логине медцентра
```

**Шаги:**
1. Проверить `/api/medcenter/login` - правильно ли сохраняется `user_type`
2. Проверить `require_auth` - правильно ли читается `user_type` из сессии
3. Добавить дополнительное логирование для диагностики

#### 1.2 Исправить загрузку и просмотр справки
```python
# app.py
# - POST /api/donor/medical-certificate (загрузка)
# - GET /api/medical-center/donors/<id>/medical-certificate/view (просмотр)
```

**Проблемы:**
- Drag-n-drop не работает (только кнопка)
- Просмотр не открывается
- Ошибка при загрузке

**Решение:**
- Проверить `uploadCertificate()` в `donor-dashboard.js`
- Проверить эндпоинты в `app.py`
- Добавить валидацию типов файлов (PDF, JPG, PNG)
- Добавить обработку ошибок

---

### ЭТАП 2: ДИЗАЙН И UI (срочно, модал, обрезанная информация)

#### 2.1 Исправить бейдж "Срочно"
```css
/* request-cards-v2.css */
.request-urgency.urgent {
    background: #fff3e0;  /* ✅ Светло-оранжевый фон */
    color: #e65100;       /* ✅ Оранжевый текст */
    border: 2px solid #ff9800; /* 🆕 ДОБАВИТЬ РАМКУ */
}
```

#### 2.2 Уменьшить модал создания запроса
```css
/* request-form.css */
#create-request-modal .modal-dialog {
    max-height: 90vh;    /* 🆕 Ограничить высоту */
    overflow-y: auto;    /* 🆕 Прокрутка */
}

.urgency-selector {
    max-height: 400px;   /* 🆕 Ограничить список срочности */
    overflow-y: auto;    /* 🆕 Прокрутка */
}
```

#### 2.3 Исправить обрезанную информацию в карточках
```css
/* request-cards-v2.css */
.request-card {
    max-width: 100%;     /* 🆕 Не выходить за пределы */
    overflow-x: hidden;  /* 🆕 Скрыть горизонтальный overflows */
    word-wrap: break-word; /* 🆕 Перенос длинных слов */
}

.request-body {
    overflow-x: auto;    /* 🆕 Горизонтальная прокрутка для длинного контента */
}
```

---

### ЭТАП 3: МАСШТАБИРУЕМОСТЬ (пагинация откликов)

#### 3.1 Пагинация откликов на запрос
**Проблема:** Если на запрос откликнулось 1000 доноров, все они отображаются сразу.

**Решение:**
```javascript
// medcenter-dashboard.js

// ШАГ 1: Показывать только первые 5 откликов в карточке
function renderBloodRequests(requests) {
    const MAX_MINI_RESPONSES = 5; // Показывать только 5
    
    if (request.donor_responses.length > MAX_MINI_RESPONSES) {
        // Показать первые 5 + кнопку "Показать все N откликов"
        html += `
            <button class="btn-view-all-responses" 
                    onclick="openAllResponsesModal(${request.id})">
                📋 Показать все ${request.donor_responses.length} откликов
            </button>
        `;
    }
}

// ШАГ 2: Модальное окно со всеми откликами (с пагинацией)
function openAllResponsesModal(requestId) {
    // Модал с таблицей откликов
    // - Поиск по имени/телефону
    // - Фильтр по статусу (новый/принят/отклонен)
    // - Сортировка (дата отклика, имя)
    // - Пагинация (по 20 на страницу)
}
```

**Новая таблица в базе данных:**
```sql
-- Индекс для быстрого поиска откликов
CREATE INDEX idx_donation_responses_request_id 
ON donation_responses(request_id);
```

---

### ЭТАП 4: ПЕРЕПИСКА (глобальная задача)

#### 4.1 Архитектура системы переписки

**База данных:**
```sql
-- Создать таблицу для сообщений в переписке
CREATE TABLE IF NOT EXISTS chat_messages (
    id SERIAL PRIMARY KEY,
    donor_id INT NOT NULL REFERENCES users(id),
    medcenter_id INT NOT NULL REFERENCES medical_centers(id),
    sender_type VARCHAR(20) NOT NULL CHECK (sender_type IN ('donor', 'medcenter')),
    message TEXT NOT NULL,
    is_read BOOLEAN DEFAULT FALSE,
    created_at TIMESTAMP DEFAULT NOW()
);

-- Индексы для быстрого поиска
CREATE INDEX idx_chat_donor ON chat_messages(donor_id);
CREATE INDEX idx_chat_medcenter ON chat_messages(medcenter_id);
CREATE INDEX idx_chat_created ON chat_messages(created_at DESC);
```

**Backend API:**
```python
# app.py

# 1. Получить список чатов (для медцентра)
@app.route('/api/medcenter/chats', methods=['GET'])
@require_auth(user_type='medcenter')
def get_medcenter_chats():
    # Вернуть список доноров, с которыми есть переписка
    # + количество непрочитанных сообщений для каждого
    pass

# 2. Получить историю переписки с конкретным донором
@app.route('/api/medcenter/chats/<int:donor_id>', methods=['GET'])
@require_auth(user_type='medcenter')
def get_chat_history(donor_id):
    # Вернуть все сообщения с донором (пагинация: 50 последних)
    pass

# 3. Отправить сообщение донору
@app.route('/api/medcenter/chats/<int:donor_id>/send', methods=['POST'])
@require_auth(user_type='medcenter')
def send_chat_message(donor_id):
    # Сохранить сообщение + отправить уведомление в Telegram
    pass

# 4. Получить чаты (для донора)
@app.route('/api/donor/chats', methods=['GET'])
@require_auth(user_type='donor')
def get_donor_chats():
    pass

# 5. История переписки для донора
@app.route('/api/donor/chats/<int:medcenter_id>', methods=['GET'])
@require_auth(user_type='donor')
def get_donor_chat_history(medcenter_id):
    pass

# 6. Отправить сообщение медцентру
@app.route('/api/donor/chats/<int:medcenter_id>/send', methods=['POST'])
@require_auth(user_type='donor')
def send_donor_chat_message(medcenter_id):
    pass

# 7. Отметить сообщения как прочитанные
@app.route('/api/chats/<int:chat_id>/read', methods=['PUT'])
@require_auth()
def mark_as_read(chat_id):
    pass
```

**Frontend (Донор):**
```javascript
// donor-dashboard.js

// Секция "Сообщения" уже есть, нужно добавить:
// 1. Список чатов (с какими медцентрами есть переписка)
// 2. Окно чата с конкретным медцентром
// 3. Форму для отправки сообщения
// 4. Уведомления о новых сообщениях (polling каждые 30 сек)
```

**Frontend (Медцентр):**
```javascript
// medcenter-dashboard.js

// Новая секция "Переписки с донорами"
// 1. Список доноров (с кем есть переписка)
// 2. Поиск донора по имени/телефону
// 3. Окно чата с выбранным донором
// 4. Форма для отправки сообщения
// 5. Уведомления о новых сообщениях
```

**Telegram интеграция:**
```python
# telegram_bot.py

def send_chat_message_notification(telegram_id, sender_name, message_preview):
    """Отправить уведомление о новом сообщении в чате"""
    text = f"""
💬 Новое сообщение от {sender_name}

{message_preview}

👉 Ответить: https://ваш-сайт.by/dashboard
"""
    bot.send_message(telegram_id, text)
```

**UI/UX чата:**
```
┌─────────────────────────────────────────────────────┐
│ 💬 ПЕРЕПИСКА С ДОНОРОМ: Войтехович Вадим Олегович  │
├─────────────────────────────────────────────────────┤
│                                                     │
│  [ДОН] Здравствуйте, могу ли я сдать кровь?       │
│        10:15                                        │
│                                                     │
│                  Да, ждём вас завтра в 9:00 [МЦ]   │
│                                          10:17      │
│                                                     │
│  [ДОН] Спасибо, буду!                              │
│        10:18                                        │
│                                                     │
├─────────────────────────────────────────────────────┤
│ [                 Введите сообщение...           📎]│
│                                         [Отправить] │
└─────────────────────────────────────────────────────┘
```

---

## 📊 ПРИОРИТИЗАЦИЯ ЗАДАЧ

### 🔴 КРИТИЧНО (делать СЕЙЧАС)
1. **Исправить 403 для медцентра** (5-10 минут)
2. **Исправить загрузку справки** (30-60 минут)
3. **План переписки** (документация, 30 минут)

### 🟡 ВАЖНО (делать СЕГОДНЯ)
4. **Исправить бейдж "Срочно"** (5 минут)
5. **Уменьшить модал создания запроса** (15 минут)
6. **Исправить обрезанную информацию** (30 минут)

### 🟢 МОЖНО ПОДОЖДАТЬ (следующая итерация)
7. **Пагинация откликов** (2-3 часа)
8. **Реализация переписки** (1-2 дня)

---

## ⏱️ ОЦЕНКА ВРЕМЕНИ

| Задача | Время | Сложность |
|--------|-------|-----------|
| 403 для медцентра | 10 мин | 🟢 Низкая |
| Загрузка справки | 60 мин | 🟡 Средняя |
| Бейдж "Срочно" | 5 мин | 🟢 Низкая |
| Модал создания запроса | 15 мин | 🟢 Низкая |
| Обрезанная информация | 30 мин | 🟡 Средняя |
| Пагинация откликов | 3 ч | 🔴 Высокая |
| Переписка (полностью) | 2 дня | 🔴 Высокая |

**Итого (критичное):** 1.5 часа  
**Итого (важное):** 1 час  
**Итого (полностью):** ~3 дня

---

## ✅ ЧЕК-ЛИСТ ВЫПОЛНЕНИЯ

- [ ] 1. Исправить 403 для медцентра
- [ ] 2. Исправить загрузку справки
- [ ] 3. Исправить бейдж "Срочно"
- [ ] 4. Уменьшить модал создания запроса
- [ ] 5. Исправить обрезанную информацию
- [ ] 6. Создать миграцию БД для переписки
- [ ] 7. Реализовать backend API для переписки
- [ ] 8. Реализовать frontend для переписки (донор)
- [ ] 9. Реализовать frontend для переписки (медцентр)
- [ ] 10. Интегрировать Telegram уведомления
- [ ] 11. Реализовать пагинацию откликов
- [ ] 12. Протестировать всё

---

## 🚀 НАЧИНАЕМ!
